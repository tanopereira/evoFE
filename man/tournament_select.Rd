% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evolve.R
\name{tournament_select}
\alias{tournament_select}
\title{Selects the individual with the highest fitness among a randomly chosen tournament of size \code{k}.}
\usage{
tournament_select(pop, k = 3)
}
\arguments{
\item{pop}{List of candidate individual objects (each with a numeric \code{fitness} element).}

\item{k}{Integer tournament size (number of candidates drawn at random).}
}
\value{
The winning candidate individual object from \code{pop}.
}
\description{
Selects the individual with the highest fitness among a randomly chosen tournament of size \code{k}.
}
\examples{
\donttest{
pop <- list(
  list(fitness = 0.5),
  list(fitness = 0.8),
  list(fitness = 0.2)
)
best <- tournament_select(pop, k = 2)
}
}
\keyword{internal}
