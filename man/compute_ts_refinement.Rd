% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{compute_ts_refinement}
\alias{compute_ts_refinement}
\title{Temperature Scaled Refinement Metric}
\usage{
compute_ts_refinement(
  y_true,
  y_pred,
  task = "classification",
  num_class = NULL,
  alpha = 1,
  is_logits = FALSE
)
}
\arguments{
\item{y_true}{Numeric vector of true labels (0/1 for classification, or 0 to C-1 for multiclass classification).}

\item{y_pred}{Numeric vector or matrix of predicted probabilities (or logits, if \code{is_logits = TRUE}).}

\item{task}{Character. Either \code{"classification"} (binary) or \code{"multiclass"}.}

\item{num_class}{Integer. Number of classes (required for multiclass).}

\item{alpha}{Numeric. Laplace smoothing parameter (default is 1).}

\item{is_logits}{Logical. If \code{TRUE}, the input predictions \code{y_pred} are treated directly as prediction margins (logits). If \code{FALSE}, they are treated as probabilities and converted to logits.}
}
\value{
Numeric. The minimized smoothed log-loss.
}
\description{
Computes the Temperature Scaled Refinement (TS-Refinement) metric for binary or multiclass classification.
The metric finds the temperature $T$ that minimizes the Laplace-smoothed log-loss of the temperature-scaled prediction margins (logits).
}
