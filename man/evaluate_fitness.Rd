% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluate_fitness}
\alias{evaluate_fitness}
\title{Evaluate the fitness of an individual}
\usage{
evaluate_fitness(
  ind,
  data,
  target_col,
  task = "classification",
  cv_folds = 3,
  evaluation_strategy = "cv",
  split_ids = NULL,
  shared_splits = NULL,
  evaluator = "lightgbm",
  fold_ids = NULL,
  shared_folds = NULL,
  shared_full = NULL,
  state_cache = NULL,
  threads = 2,
  metric = "default",
  verbose = FALSE,
  allow_prune = TRUE,
  complexity_penalty = 0,
  complexity_mode = "bic_dynamic",
  complexity_floor = 0.2,
  running_best_fitness = NULL,
  baseline_fitness = NULL,
  n_samples = NULL,
  ...
)
}
\arguments{
\item{ind}{An evo_individual object.}

\item{data}{A data.frame or data.table containing the dataset.}

\item{target_col}{Name of the target column.}

\item{task}{"classification" or "regression".}

\item{cv_folds}{Number of cross-validation folds.}

\item{evaluation_strategy}{Character string, either "cv" (cross-validation) or "split" (train/validation split).}

\item{split_ids}{Optional vector of pre-defined split assignments (e.g. \code{c("train", "train", "val", "holdout", "train")}). Must have the same length as the number of rows in \code{data} and contain only "train", "val", or "holdout" labels.}

\item{shared_splits}{Optional list of shared data.table splits for in-place caching.}

\item{evaluator}{The ML model to use ("lightgbm", "xgboost", "catboost", or a custom registered evaluator name).}

\item{fold_ids}{Optional vector of pre-defined fold assignments.}

\item{shared_folds}{Optional list of shared data.table CV folds for in-place caching.}

\item{shared_full}{Optional data.table of the full dataset for in-place caching.}

\item{state_cache}{Optional environment to cache full-dataset fitted states of stateful transformers.}

\item{threads}{Number of threads to use for parallel execution (default 2)}

\item{metric}{The metric to optimize ("default", "auc", "f1", "mae", or a custom function).}

\item{verbose}{Logical indicating if progress should be printed.}

\item{allow_prune}{Logical. If TRUE, genes that fail application are skipped instead of failing the entire individual.}

\item{complexity_penalty}{Non-negative numeric multiplier for complexity penalty (default 0).
When set to 1.0, applies standard BIC parsimony pressure. Default \code{0} disables the penalty.}

\item{complexity_mode}{Character. Complexity penalty strategy: "bic_dynamic" (default, scales
with dataset size and relaxes as fitness approaches the ideal), "bic" (scales with dataset size,
constant across generations), or "none" (no penalty).}

\item{complexity_floor}{Numeric in \code{[0, 1]}. Minimum safety floor factor for dynamic BIC penalty (default \code{0.20}).}

\item{running_best_fitness}{Optional numeric. Current running best fitness for dynamic BIC.}

\item{baseline_fitness}{Optional numeric. Generation 0 baseline fitness for dynamic BIC.}

\item{n_samples}{Optional integer. Dataset sample size N for BIC calculations. Defaults to nrow(data).}

\item{...}{Additional arguments passed to the underlying evaluator training functions.}
}
\value{
The input \code{evo_individual} with its \code{fitness} field set to
  the computed score (higher is better), \code{importances} set to a named
  numeric vector of feature importances, \code{holdout_fitness} set to
  \code{NULL}, and \code{genes} updated with fitted transformer states.
}
\description{
Trains a model using the features specified by the individual's recipe and evaluates
performance using cross-validation or train/val split.
}
